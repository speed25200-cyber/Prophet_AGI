# Reprise expérimentale — 19 septembre 2026

## État et portée

La branche v0.3 (`ef07752`) et la branche d'expériences CPU (`aac1c0a`) étaient
divergentes. Cette reprise réunit leurs mécanismes et leurs tests. Aucun résultat
historique de qualité du README n'a été reproduit lors de cette reprise. Aucun modèle
compétitif, apprentissage continu transférable ou AGI n'est revendiqué.

Le clone de la branche par défaut ne contenait pas `prophet/data`, bien que les scripts
l'importent. v0.3 restaure une partie du package, mais pas le lecteur de corpus utilisé
par les scripts plus récents. `prophet/data/corpus.py` est donc reconstruit : fichiers
JSONL/TXT, flux Hub à révision épinglée, filtrage, décontamination, plafond d'époques,
phases et reprise. Les empreintes incluent contenu, tokenizer, politique des tokens de
contrôle, décontamination, mélange et forme des batches. Les données et poids restent
hors Git. Pour les grands corpus, l'index est proportionnel au nombre de documents ; le
contrôle initial lit tous les octets pour établir leur identité.

Les identifiants de contrôle déjà présents dans v0.3 sont conservés ; les contrôles
agentiques sont ajoutés dans la plage réservée. Les anciens artefacts ayant exactement
le préfixe connu sont lisibles. Les poids et tokenizers des expériences historiques
absents du dépôt doivent être vérifiés ensemble avant de prétendre les reprendre.

## Réconciliation des contrats

- La mémoire consolidée se monte par défaut **après la normalisation finale**, dans
  l'espace effectivement appris par la consolidation. Le montage `coda` utilise des
  indices locaux explicites, et fonctionne aussi sur le tronc non récurrent.
- Les sondes de profondeur lisent des copies des caches. La halte adaptative avec cache
  incrémental reste refusée : voir la limitation causale v0.3. La profondeur fixe et les
  plafonds de profondeur par token conservent leurs tests propres.
- Les états de session valident aussi les registres d'attention et ne confondent pas
  leurs valeurs mutables avec l'identité des poids.
- La reprise du trainer valide le lecteur polymorphe avant mutation, y compris les
  lecteurs à phases. Le CPU conserve le balayage par blocs ; le GPU conserve le contrat
  d'état transposé de FLA. L'extra `gpu` épingle `fla-core==0.5.2` sur Linux.

## R04 : réparer l'expérience avant de la financer

La configuration bouclée précédente avait environ 220M paramètres, sous le seuil de
350M **exigé par le plan du projet**. Elle échantillonnait 1–8 passes, utilisait des
sondes de halte et tronquait le gradient ; le témoin parcourait 16 blocs de cœur une
fois. L'égalité des profondeurs par défaut ne prouvait pas une égalité du calcul
d'entraînement.

Les configurations générées isolent désormais le partage de poids :

| Quantité | Boucle | Pile non partagée |
|---|---:|---:|
| Largeur | 1792 | 1792 |
| Paramètres résidents | 374 688 512 | 920 675 072 |
| Blocs prélude / cœur / coda | 2 / 4 / 2 | 2 / 16 / 2 |
| Passes du cœur, entraînement et inférence | 4 | 1 |
| Blocs exécutés par token | 20 | 20 |
| Gradient à travers toutes les passes | oui | oui |
| Halte / MTP / tête de confiance | désactivés | désactivés |
| Mémoire entraînement estimée, 2048 tokens/batch | 4,71 Gio | 10,81 Gio |

Même séquence de blocs exécutés ne signifie pas même temps GPU : l'optimiseur, les
écritures mémoire et les lancements de kernels diffèrent. Mesurer tokens/s, pic mémoire
**avec optimiseur**, qualité/FLOPs et qualité/seconde. Ce seuil de 350M est une contrainte
du protocole existant, pas une garantie scientifique qu'un effet commence à cette taille.
L'ablation à paramètres égaux et les trois graines restent à exécuter.

## Recherche primaire vérifiée

Consultation du 19 septembre 2026 :

- [Huginn](https://arxiv.org/abs/2502.05171) rapporte 3,5B paramètres et 800B tokens.
  Il motive le calcul latent récurrent ; son résultat ne valide pas Prophet à 375M.
- [Adaptive Depth in Looped Transformers](https://arxiv.org/abs/2607.20519) sépare la
  formation de trajectoires et leur lecture. Cela justifie de comparer une supervision
  à poids fixes à la perte pondérée par halte, avant d'adopter celle-ci.
- [Universal Transformers Need Memory](https://arxiv.org/abs/2604.21999) étudie des
  tokens mémoire sur Sudoku. Cela motive l'ablation du bloc-notes éphémère proposé en
  v0.3, sans établir son bénéfice pour un modèle de langage causal.
- [FLA officiel](https://github.com/fla-org/flash-linear-attention) fournit les noyaux.
  Le contrat consulté utilise `[B,T,H,D]` et un état `[B,H,K,V]`. La conformité des
  sorties, états **et gradients** doit être mesurée sur l'accélérateur réel.

## Vérifications locales

Windows, Python 3.14, PyTorch 2.14.0+cpu, NumPy 2.5.3, pytest 9.1.1.

- Suite complète : **592 réussites, 3 tests GPU sautés**, 117,51 s.
- Après ajout de l'équivalence d'entraînement à phases : **12/12** tests de reprise
  ciblés passent ; le nouveau test compare tous les tenseurs des modèles interrompu et
  ininterrompu, exactement égaux après la frontière de phase.
- Contrôles Ruff sur les fichiers du CI et nouveaux fichiers : réussis.
- CLI corpus local généré pour vérifier le pipeline, 6,4M paramètres, 512 tokens :
  arrêt au pas 5/8, reprise depuis le pas 5, fin au pas 8. Perte 7,7523 au premier pas,
  4,8561 au dernier ; ce corpus minuscule ne mesure aucune qualité de langage.
  Checkpoint de 53,8 Mo. Artefacts locaux sous `outputs/recovery-smoke/`, hors Git.

## Validation sur le premier A100 réel

Colab a attribué un **NVIDIA A100-SXM4-40GB**, et non le modèle 80 Go du budget
initial : Python 3.13.15, PyTorch 2.11.0+cu128, `fla-core==0.5.2`.
Le CI Linux de la révision `3528143` passe les **595 tests CPU** ; les cinq tests
CUDA sont exécutés séparément dans Colab.

Trois défauts numériques ont été isolés sans augmenter les tolérances GPU :

1. Sous autocast CUDA, `normalize` produisait des clés FP32 face à des valeurs
   BF16. Le noyau Triton refusait ce mélange. La précision des entrées est explicite.
2. Les multiplications de la référence étaient elles aussi reconverties par
   autocast, malgré les appels `.float()`. L'accumulation de référence est maintenant
   protégée ; l'accumulation fusionnée emploie également des entrées FP32, les
   projections restant BF16 pendant l'entraînement mixte.
3. Le solveur triangulaire fusionné de FLA 0.5.2, par blocs de 64, impose `tf32`
   dans son code. `TRITON_F32_DEFAULT=ieee` ou `tf32x3` ne change pas ce produit
   explicite. Le chemin public `chunk_size=32` utilise le solveur générique précis.
   Prophet le sélectionne, avec `tf32x3` par défaut pour les autres produits FP32.
   Voir [le code FLA](https://github.com/fla-org/flash-linear-attention/blob/main/fla/ops/gated_delta_rule/chunk_fwd.py)
   et [les précisions de produit Triton](https://triton-lang.org/main/python-api/generated/triton.language.dot.html).

Avec la version initiale corrigée uniquement pour les types, trois des cinq tests
GPU échouaient encore. Avec des entrées de récurrence FP32 mais le solveur 64,
les sorties, états, décodage et deux pas d'entraînement passaient ; le gradient du
biais d'oubli gardait une erreur L2 relative de **0,353 % en FP32** (limite 0,2 %)
et **4,422 % en précision mixte** (limite 3 %). Le solveur 32 passe les deux
comparaisons de gradients ciblées, avec les mêmes limites.

La révision publiée **`35281438b3862585bc680f2e60d4a6e940376663`** passe ensuite
**les 5 tests GPU en 163,49 s**, avec ses réglages par défaut et sans remplacement
de fonctions dans le notebook. Ce temps inclut la compilation des formes encore
absentes du cache ; ce n'est pas un temps d'entraînement ni un débit.

Le [notebook reproductible](../notebooks/validate_a100.ipynb) fixe la révision,
refuse un environnement sans CUDA/FLA et ne profile R04 qu'après la réussite de
tous les tests GPU. Le profilage compte les mises à jour Muon/AdamW, exclut deux
pas de chauffe et sauvegarde les versions, le débit et le pic mémoire en Gio.
Il utilise des tokens aléatoires : il mesure le coût, pas la qualité.

### Coûts R04 mesurés

Séquences de 2048 tokens, checkpointing activé, Muon et AdamW réellement exécutés.
Deux pas de chauffe exclus ; trois pas mesurés à batch 1, cinq à batch 8. Les
[mesures exactes et versions](experiments/2026-09-19-a100.json) sont conservées.

| Configuration | Séquences / lot | Tokens/s | Secondes / pas | Pic alloué (Gio) | Estimation initiale (Gio) |
|---|---:|---:|---:|---:|---:|
| Boucle 374,69M | 1 | 5 048 | 0,406 | 5,02 | 4,71 |
| Pile 920,68M | 1 | 3 477 | 0,589 | 11,14 | 10,81 |
| Boucle 374,69M | 8 | 8 529 | 1,921 | 16,74 | 6,81 |
| Pile 920,68M | 8 | 7 778 | 2,106 | 20,84 | 12,91 |

Les deux formes à batch 8 tiennent dans les **39,49 Gio réellement disponibles**.
Le gain de débit mesuré de la boucle est d'environ 9,7 % à ce batch ; cela ne
compare ni qualité ni convergence. L'optimiseur travaille sur moins de paramètres,
bien que les deux modèles exécutent 20 blocs/token.

À débit constant, 100M tokens représentent environ **3,26 h / 3,57 h** à batch 8,
hors compilation, lecture des données, validation et checkpoints. Ce sont des
extrapolations de profilages courts, pas des entraînements de 100M tokens effectués.

L'estimateur mémoire est raisonnable à batch 1 mais **sous-estime de 2,46× / 1,61×**
à batch 8. Il ne faut donc pas dimensionner les grands lots avec lui seul. Les
allocations du vocabulaire et des pertes matérialisées, ainsi que les états de la
récurrence, doivent être intégrées au modèle de coût ou réduites, puis remesurées.
Le contrôle numérique initial imposait `k=3` aux deux configurations : il testait
16 blocs exécutés pour la boucle et 52 pour la pile, au lieu de leurs 20 blocs
configurés. Les pas chronométrés utilisaient déjà les bonnes profondeurs 4 et 1.
Le vérificateur a été corrigé pour respecter la profondeur configurée et imposer
une référence sans TF32, indépendamment des réglages du trainer.

L'[audit supplémentaire sur trois graines](experiments/2026-09-19-a100-reference.json),
révision `0d9ac74`, passe les six comparaisons sorties/états à profondeur réelle :
erreur maximale **4,25e-5 pour la boucle**, **4,35e-4 pour la pile**, limite 2e-3.
Il s'agit de tests numériques à l'initialisation, pas des ablations de qualité
après entraînement sur trois graines, qui restent à réaliser.

## Travail restant vers l'objectif complet

### Audit des sources du 19 septembre

`scripts/verify_datasets.py` a pu interroger le Hub. Le mélange proposé n'est pas
encore prêt pour une campagne : les trois dépôts Nemotron sont à accès restreint,
plusieurs cartes n'exposent pas de licence exploitable par le vérificateur, et les
déclarations de Cosmopedia v2 (Apache-2.0) et OPC annealing (MIT) diffèrent du champ
`odc-by` exposé par leur dépôt. Une licence de dépôt ne suffit pas à trancher les
conditions de chacun de ses sous-ensembles ; ces entrées restent à auditer.
L'accès restreint n'a pas été accepté automatiquement et aucun de ces corpus n'a
été utilisé pour entraîner le modèle durant cette reprise.

`scripts/verify_donors.py` confirme les dimensions comparées de Qwen3 0.6B, 1.7B,
4B et SmolLM3 3B ; Llama 3.2 1B n'a pas pu être lu. Le vérificateur omettait
`rope_theta` : ce contrôle et son test de régression ont été ajoutés, puis l'audit
relancé avec les mêmes quatre correspondances. Cela ne constitue pas encore une
vérification complète de la conversion : aucun poids donneur n'a été chargé ici.

### Étapes suivantes

1. Maintenir le contrôle FLA sur chaque changement de dépendances ou de matériel :
   sorties, états, gradients, bf16, décodage et checkpointing. Il passe sur l'A100
   testé ici ; bloquer la campagne en cas de nouvel écart sans affaiblir la tolérance.
2. Poursuivre l'audit du mélange complet. Un [pilote FineWeb-Edu figé](13_REAL_TEXT_PILOT.md)
   est maintenant téléchargé, dédupliqué et séparé de sa validation, et son tokenizer
   est entraîné. Il ne couvre pas encore le mélange multilingue/code final.
3. Exécuter les ablations R04 corrigées puis trajectoires à poids fixes, bloc-notes
   causal et baselines CoT avec budget de calcul comparable. Rapporter tous les échecs.
4. Construire et éprouver journal/index reconstruisible, no-match, corrections,
   suppression, adaptateurs bornés et promotion transactionnelle par les portes v0.3.
5. Mesurer le transfert après reset, la rétention, le coût et la robustesse de l'agent.
6. Produire les poids mini et la conversion du donneur, post-entraînement, puis mesurer
   les variantes RTX/Mac/iPhone et leur quantification sur les appareils concernés.

Ces travaux constituent toujours l'objectif ; restaurer le dépôt et préparer une
expérience ne suffit pas à le déclarer atteint.
